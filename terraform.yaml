tool:
  terraform:
    installations:
      - name: "terraform"
        home: "/var/jenkins_home/workspace/"
        properties:
          - installSource:
              installers:
                - terraformInstaller:
                    id: "1.0.4-linux-amd64"
